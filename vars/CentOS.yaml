---
# vars file for dgnest.monit (CentOS specific)

_monit_package_dependencies:
  - build-essential


_monit_dependencies:
  - name: build-essential
    version: 1.0.0

_monit_packages:
  - packages
